# =============================================================================
# NFS-SERVER.YML - Cài đặt NFS Server trên DB+NFS Server
# =============================================================================
# Chạy: ansible-playbook -i inventory/hosts.ini playbooks/nfs-server.yml
# =============================================================================

---
- name: Setup NFS Server
  hosts: nfs_server
  become: yes
  gather_facts: yes

  vars:
    nfs_export_path: "/srv/nfs/uploads"
    nfs_allowed_cidr: "10.0.0.0/16"  # VPC CIDR

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Install NFS server packages
      apt:
        name:
          - nfs-kernel-server
          - nfs-common
        state: present

    - name: Create NFS export directory
      file:
        path: "{{ nfs_export_path }}"
        state: directory
        mode: '0777'
        owner: nobody
        group: nogroup

    - name: Configure NFS exports
      lineinfile:
        path: /etc/exports
        line: "{{ nfs_export_path }} {{ nfs_allowed_cidr }}(rw,sync,no_subtree_check,no_root_squash)"
        state: present
      notify: reload nfs exports

    - name: Ensure NFS server is running
      systemd:
        name: nfs-kernel-server
        state: started
        enabled: yes

    - name: Apply NFS exports
      command: exportfs -ra

    - name: Verify NFS exports
      command: exportfs -v
      register: nfs_exports
      changed_when: false

    - name: Display NFS server info
      debug:
        msg: |
          ============================================
          📁 NFS SERVER READY!
          ============================================
          Export Path: {{ nfs_export_path }}
          Private IP: {{ ansible_default_ipv4.address }}
          Allowed Network: {{ nfs_allowed_cidr }}
          
          NFS Exports:
          {{ nfs_exports.stdout }}
          
          📝 Để mount từ K8s, dùng:
          server: {{ ansible_default_ipv4.address }}
          path: {{ nfs_export_path }}
          ============================================

  handlers:
    - name: reload nfs exports
      command: exportfs -ra
