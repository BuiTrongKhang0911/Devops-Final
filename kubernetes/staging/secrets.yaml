# =============================================================================
# STAGING SECRETS - Sensitive data (passwords)
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: staging
type: Opaque
stringData:
  POSTGRES_PASSWORD: "PLACEHOLDER_DB_PASSWORD"
  SPRING_DATASOURCE_PASSWORD: "PLACEHOLDER_DB_PASSWORD"
