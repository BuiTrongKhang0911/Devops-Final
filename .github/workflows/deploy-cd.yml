# =============================================================================
# DEPLOY-CD.YML - Continuous Deployment Pipeline
# =============================================================================
# Mục đích: Deploy ứng dụng lên Kubernetes cluster
# Trigger: Tự động chạy khi "Build & Release Docker" thành công
# =============================================================================

name: Continuous Deployment (CD)

# Chỉ kích hoạt khi workflow "Build & Release Docker" hoàn thành
on:
  workflow_run:
    workflows: ["Build & Release Docker"]
    types:
      - completed

env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}

jobs:
  # ===========================================================================
  # JOB: DEPLOY TO KUBERNETES
  # ===========================================================================
  deploy:
    name: Deploy to Kubernetes
    runs-on: ubuntu-latest
    # Chỉ chạy nếu CI workflow thành công
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      # =========================================================================
      # BƯỚC 1: Checkout code (để lấy kubernetes manifests)
      # =========================================================================
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      # =========================================================================
      # BƯỚC 2: Setup SSH key
      # =========================================================================
      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.K8S_SSH_KEY }}" > ~/.ssh/k8s_key
          chmod 600 ~/.ssh/k8s_key
          ssh-keyscan -H ${{ secrets.K8S_HOST }} >> ~/.ssh/known_hosts 2>/dev/null || true

      # =========================================================================
      # BƯỚC 3: Update image tags và secrets trong manifests
      # =========================================================================
      - name: Update Image Tags and Secrets
        run: |
          # Lấy SHA từ workflow_run event
          COMMIT_SHA="${{ github.event.workflow_run.head_sha }}"
          SHORT_SHA=$(echo "$COMMIT_SHA" | cut -c1-7)
          
          echo "📦 Deploying commit: $COMMIT_SHA"
          echo "📦 Short SHA: $SHORT_SHA"
          
          # Update backend image
          sed -i "s|DOCKER_USERNAME/document-management-backend:latest|${{ env.DOCKER_USERNAME }}/document-management-backend:sha-${SHORT_SHA}|g" \
            kubernetes/base/backend/deployment.yaml
          
          # Update frontend image
          sed -i "s|DOCKER_USERNAME/document-management-frontend:latest|${{ env.DOCKER_USERNAME }}/document-management-frontend:sha-${SHORT_SHA}|g" \
            kubernetes/base/frontend/deployment.yaml
          
          # Update secrets (lấy từ GitHub Secrets)
          sed -i "s|PLACEHOLDER_DB_PASSWORD|${{ secrets.DB_PASSWORD }}|g" \
            kubernetes/secrets.yaml
          
          echo "✅ Updated images to tag: sha-${SHORT_SHA}"
          echo "✅ Updated secrets from GitHub Secrets"

      # =========================================================================
      # BƯỚC 4: Copy manifests lên K8s server
      # =========================================================================
      - name: Copy Manifests to K8s Server
        run: |
          scp -i ~/.ssh/k8s_key -r kubernetes/ ubuntu@${{ secrets.K8S_HOST }}:~/

      # =========================================================================
      # BƯỚC 5: Deploy to Kubernetes
      # =========================================================================
      - name: Deploy to Kubernetes
        run: |
          ssh -i ~/.ssh/k8s_key ubuntu@${{ secrets.K8S_HOST }} << 'EOF'
            set -e
            
            echo "🚀 Starting Kubernetes Deployment..."
            
            # Apply namespace first
            kubectl apply -f kubernetes/namespace.yaml
            
            # Apply configs and secrets
            kubectl apply -f kubernetes/configmap.yaml
            kubectl apply -f kubernetes/secrets.yaml
            
            # Apply database (postgres)
            kubectl apply -f kubernetes/base/postgres/
            
            # Wait for postgres to be ready
            echo "⏳ Waiting for PostgreSQL to be ready..."
            kubectl wait --for=condition=ready pod -l app=postgres -n devops-final --timeout=120s || true
            
            # Apply backend
            kubectl apply -f kubernetes/base/backend/
            
            # Apply frontend
            kubectl apply -f kubernetes/base/frontend/
            
            # Apply ingress
            kubectl apply -f kubernetes/ingress.yaml
            
            # Force rollout để pull image mới
            kubectl rollout restart deployment/backend -n devops-final
            kubectl rollout restart deployment/frontend -n devops-final
            
            # Wait for rollout
            echo "⏳ Waiting for rollout to complete..."
            kubectl rollout status deployment/backend -n devops-final --timeout=180s
            kubectl rollout status deployment/frontend -n devops-final --timeout=180s
            
            echo "✅ Deployment completed successfully!"
            
            # Show status
            echo ""
            echo "📊 Current Pod Status:"
            kubectl get pods -n devops-final
            echo ""
            echo "📊 Current Services:"
            kubectl get svc -n devops-final
          EOF

      # =========================================================================
      # BƯỚC 6: Print Deployment Summary
      # =========================================================================
      - name: Print Deployment Summary
        run: |
          COMMIT_SHA="${{ github.event.workflow_run.head_sha }}"
          SHORT_SHA=$(echo "$COMMIT_SHA" | cut -c1-7)
          echo "============================================="
          echo "🎉 DEPLOYMENT COMPLETED!"
          echo "============================================="
          echo "Commit: $COMMIT_SHA"
          echo "Backend Image: ${{ env.DOCKER_USERNAME }}/document-management-backend:sha-${SHORT_SHA}"
          echo "Frontend Image: ${{ env.DOCKER_USERNAME }}/document-management-frontend:sha-${SHORT_SHA}"
          echo "K8s Server: ${{ secrets.K8S_HOST }}"
          echo "Namespace: devops-final"
          echo "============================================="

  # ===========================================================================
  # JOB: NOTIFY ON FAILURE (Optional)
  # ===========================================================================
  notify-ci-failed:
    name: Notify CI Failed
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    
    steps:
      - name: CI Failed Notification
        run: |
          echo "============================================="
          echo "❌ CI PIPELINE FAILED!"
          echo "============================================="
          echo "CD Pipeline skipped because CI failed."
          echo "Please check the 'Build & Release Docker' workflow."
          echo "Commit: ${{ github.event.workflow_run.head_sha }}"
          echo "============================================="
